{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"TLS Impersonate Proxy","text":"<p>Fork Attribution</p> <p>This project is a fork of the original source project hauxir/tls-impersonate-proxy maintained at ndejong/tls-impersonate-proxy.</p> <p>HTTP/HTTPS proxy that impersonates browser TLS fingerprints (JA3/JA4) using <code>curl_cffi</code> to defeat CDN fingerprint-based blocking of non-browser clients.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>TLS Impersonation: Disguises client TLS fingerprints as standard browsers (Chrome or Firefox).</li> <li>Centralized Header Enrichment: Automatically decorates incoming requests from plain/command-line clients (e.g. <code>curl</code>, <code>requests</code>) with matching browser headers (User-Agent, Accept, Chromium Client Hints) corresponding to the active TLS profile.</li> <li>Connection Keep-Alive &amp; Session Pooling: Reuses TLS sessions and upstream TCP connections to optimize latency and handle high-concurrency requests.</li> <li>Fast Dynamic Cert Generation: Dynamic certificate generation using fast Elliptic Curve (ECDSA P-256) cryptography with leaf key reuse.</li> </ul>"},{"location":"#quick-start-docker","title":"Quick Start (Docker)","text":"<p>Spin up the proxy container in one line:</p> <pre><code>docker run --rm -p 8899:8899 \\\n  -v tls-proxy-ca-certs:/root/.config/tls-impersonate-proxy \\\n  ghcr.io/ndejong/tls-impersonate-proxy:latest\n</code></pre> <p>Once started, configure your client to use the proxy:</p> <pre><code># Export the generated CA cert to trust the proxy\nexport SSL_CERT_FILE=~/.config/tls-impersonate-proxy/ca.crt\n\n# Issue request through the proxy\ncurl -x http://127.0.0.1:8899 https://cloudflare.com\n</code></pre>"},{"location":"#capabilities","title":"Capabilities","text":"Capability What it does Benefit TLS Fingerprinting Matches JA3/JA4/HTTP2 fingerprints with real browsers Defeats Cloudflare, Akamai, and Imperva WAF blocks MITM CONNECT Decryption Decrypts and re-signs HTTPS traffic using local CA Allows inspection, headers modification, and proxying of TLS streams Header Enrichment Auto-injects appropriate browser headers &amp; Client Hints Ensures User-Agent and TLS fingerprint profiles align out-of-the-box Session Pooling Maintains queue-based reusable <code>curl_cffi</code> sessions Boosts performance for concurrent request bursts to ~70+ RPS P-256 Cryptography Reuses a static leaf private key for ECDSA cert creation Reduces leaf certificate dynamic issuance time to <code>&lt;1ms</code>"},{"location":"#guided-tour","title":"Guided Tour","text":"<ul> <li>Installation Guide: System setup, CLI parameters, and Docker integrations.</li> <li>How It Works: Deep dive into the interception pipeline, session pool, and header decoration mechanism. ```</li> </ul>"},{"location":"install/","title":"Installation &amp; CLI Guide","text":"<p><code>tls-impersonate-proxy</code> can be run either as a local Python application or containerized using Docker.</p>"},{"location":"install/#local-installation","title":"Local Installation","text":""},{"location":"install/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 or newer</li> <li><code>pip</code> or <code>uv</code> package manager</li> </ul>"},{"location":"install/#install-via-git","title":"Install via git","text":"<pre><code>pip install git+https://github.com/ndejong/tls-impersonate-proxy.git\n</code></pre> <p>Or using uv: <pre><code>uv pip install git+https://github.com/ndejong/tls-impersonate-proxy.git\n</code></pre></p>"},{"location":"install/#docker-installation-recommended","title":"Docker Installation (Recommended)","text":"<p>Running the proxy via Docker is recommended for isolated operation and to persist certificate states easily.</p>"},{"location":"install/#local-docker-build","title":"Local Docker Build","text":"<pre><code># Build the Docker image\ndocker build -t tls-impersonate-proxy .\n\n# Run the proxy container (binding port 8899 and persisting CA certs)\ndocker run --rm -p 8899:8899 \\\n  -v tls-proxy-ca-certs:/root/.config/tls-impersonate-proxy \\\n  tls-impersonate-proxy\n</code></pre>"},{"location":"install/#docker-compose","title":"Docker Compose","text":"<p>You can spin up the service in the background using docker-compose: <pre><code>services:\n  proxy:\n    build: .\n    ports:\n      - \"8899:8899\"\n    environment:\n      - TLS_PROXY_IMPERSONATE=chrome\n      - TLS_PROXY_DEBUG=false\n    volumes:\n      - tls-proxy-ca-certs:/root/.config/tls-impersonate-proxy\n\nvolumes:\n  tls-proxy-ca-certs:\n</code></pre></p>"},{"location":"install/#command-line-interface-cli","title":"Command Line Interface (CLI)","text":"<p>Run <code>tls-impersonate-proxy --help</code> to view all available parameters:</p> <pre><code>usage: tls-impersonate-proxy [-h] [--port PORT] [--host HOST] [--impersonate IMPERSONATE] [--ca-dir CA_DIR] [--debug]\n\noptions:\n  -h, --help            show this help message and exit\n  --port PORT, -p PORT  Port to listen on (default: 8899 or TLS_PROXY_PORT)\n  --host HOST, -s HOST  Host to bind to (default: 127.0.0.1 or TLS_PROXY_HOST)\n  --impersonate IMPERSONATE, -i IMPERSONATE\n                        Browser to impersonate (chrome, firefox, etc. Default: chrome or TLS_PROXY_IMPERSONATE)\n  --ca-dir CA_DIR, -c CA_DIR\n                        Directory to store/load CA certificate and key (default: ~/.config/tls-impersonate-proxy or TLS_PROXY_CA_DIR)\n  --debug, -d           Enable verbose debug logging (unredacts URLs/hosts in logs)\n</code></pre>"},{"location":"install/#trusting-the-root-ca","title":"Trusting the Root CA","text":"<p>To allow client applications to perform HTTPS requests through the proxy without throwing SSL verification errors:</p> <ol> <li>Locate the Certificate: By default, the root certificate is created at <code>~/.config/tls-impersonate-proxy/ca.crt</code>.</li> <li>Set Environment Variables: Many tools (like <code>curl</code>, <code>wget</code>, <code>python-requests</code>, <code>httpx</code>) respect specific environment variables for trust stores:</li> </ol> <pre><code># Set CA trust path for curl/python\nexport SSL_CERT_FILE=~/.config/tls-impersonate-proxy/ca.crt\nexport REQUESTS_CA_BUNDLE=~/.config/tls-impersonate-proxy/ca.crt\n\n# Test HTTPS request through the proxy\ncurl -x http://127.0.0.1:8899 https://example.com\n</code></pre>"},{"location":"license/","title":"License","text":""},{"location":"license/#mit-license","title":"MIT License","text":"<p><code>tls-impersonate-proxy</code> is licensed under the MIT License.</p> <p>See the full license text in the LICENSE file of the repository.</p>"},{"location":"license/#copyright","title":"Copyright","text":"<ul> <li>Copyright \u00a9 2026 ndejong</li> </ul> <p>All rights reserved.</p>"},{"location":"workflow/","title":"How It Works (Architecture &amp; Workflow)","text":"<p><code>tls-impersonate-proxy</code> acts as a transparent intermediary that sits between client applications (like <code>curl</code>, <code>ffmpeg</code>, or Python HTTP clients) and the destination servers. It prevents fingerprint blocking by aligning the client's TLS handshake fingerprint with its HTTP headers.</p>"},{"location":"workflow/#1-request-interception-workflow","title":"1. Request Interception Workflow","text":"<p>Below is a diagram illustrating how the proxy handles client requests:</p> <pre><code>sequenceDiagram\n    participant Client as Client (e.g. curl)\n    participant Proxy as TLS Impersonate Proxy\n    participant Target as Target Server (CDN/WAF)\n\n    Client-&gt;&gt;Proxy: HTTP request or HTTPS CONNECT\n    alt Plain HTTP Request\n        Proxy-&gt;&gt;Proxy: Lease session from Pool\n        Proxy-&gt;&gt;Proxy: Enrich headers (Match TLS Profile)\n        Proxy-&gt;&gt;Target: Issue request (via curl_cffi with TLS Impersonation)\n        Target--&gt;&gt;Proxy: Response\n        Proxy--&gt;&gt;Client: Stream response chunk-by-chunk\n        Proxy-&gt;&gt;Proxy: Return session to Pool\n    else HTTPS CONNECT Request (MITM)\n        Proxy--&gt;&gt;Client: HTTP/1.1 200 Connection Established\n        Proxy-&gt;&gt;Proxy: Generate or lookup dynamic Leaf Cert\n        Proxy-&gt;&gt;Client: Perform local TLS Handshake (MITM)\n        Client-&gt;&gt;Proxy: Encrypted HTTP request\n        Proxy-&gt;&gt;Proxy: Lease session from Pool\n        Proxy-&gt;&gt;Proxy: Enrich headers (Match TLS Profile)\n        Proxy-&gt;&gt;Target: Issue request (via curl_cffi with TLS Impersonation)\n        Target--&gt;&gt;Proxy: Response\n        Proxy--&gt;&gt;Client: Stream response chunk-by-chunk\n        Proxy-&gt;&gt;Proxy: Return session to Pool\n    end</code></pre>"},{"location":"workflow/#2-dynamic-certificate-generation-mitm","title":"2. Dynamic Certificate Generation (MITM)","text":"<p>For HTTPS requests, the proxy establishes a Man-in-the-Middle (MITM) state to inspect headers and decrypt the client payload.</p> <ol> <li>Root CA Generation: At startup, if no CA files are found, the proxy creates a root CA private key and self-signed certificate using fast ECDSA SECP256R1 (P-256) keys.</li> <li>Leaf Certificate Issuance:<ul> <li>For each requested domain, the proxy generates a leaf certificate signed by the root CA.</li> <li>Optimization: To avoid CPU bottleneck during RSA key creation, the proxy generates an ECDSA P-256 leaf certificate and reuses a single static leaf key (<code>_LEAF_KEY</code>) globally across all generated certs. This reduces leaf cert generation times to <code>&lt;1ms</code>.</li> </ul> </li> <li>Certificate Caching: Generated <code>ssl.SSLContext</code> structures are stored in a thread-safe cache (<code>_HOST_CERT_CACHE</code>). If the cache grows beyond 256 hosts, the least recently used (LRU) context is evicted automatically.</li> </ol>"},{"location":"workflow/#3-keep-alive-connection-session-pooling","title":"3. Keep-Alive Connection &amp; Session Pooling","text":"<p>Under load (such as bursts of ~100 concurrent requests), initiating new <code>curl_cffi</code> sessions is resource-intensive due to easy handle allocation and TLS handshakes.</p> <ul> <li>Global Session Pool: The proxy manages a queue-based <code>_SESSION_POOL</code> containing reusable <code>curl_cffi</code> sessions.</li> <li>Upstream Keep-Alive: Reusing sessions maintains active TCP connections to target hosts. Subsequent requests to the same target domain completely bypass DNS resolving and TCP/TLS handshake steps.</li> <li>Resource Leasing: When a request starts, a session is leased from the pool. The proxy wraps the response's <code>close()</code> method so that the session is automatically returned to the pool only when the response stream has been fully consumed or closed by the client.</li> </ul>"},{"location":"workflow/#4-centralized-header-enrichment","title":"4. Centralized Header Enrichment","text":"<p>WAF systems (like Cloudflare or Akamai) block requests where the TLS fingerprint (e.g. JA3/JA4 indicating Chrome) does not match the HTTP <code>User-Agent</code> (e.g. <code>curl/7.81.0</code> or <code>python-requests</code>).</p> <p><code>tls-impersonate-proxy</code> intercept headers on the fly: 1. Detects if the incoming request uses a plain/command-line user-agent. 2. Injects browser-specific headers matching the active impersonation profile:     - User-Agent: e.g., Chrome 120 or Firefox 120 User-Agent.     - Accept / Language Headers: Standard browser request layouts.     - Chromium Client Hints: Injects <code>Sec-Ch-Ua</code>, <code>Sec-Ch-Ua-Mobile</code>, <code>Sec-Ch-Ua-Platform</code> and <code>Sec-Fetch-*</code> headers for Chrome impersonations. 3. The upstream request is issued via <code>curl_cffi</code> using the matching TLS profile, guaranteeing complete fingerprint alignment.</p>"}]}